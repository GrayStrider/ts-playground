console.log('Waiting...')

/**
 * Too much hassle with setting up environments, bootstrap() works good enough
 */
// await sleep(1000)

console.log('Done.')

/**
 * We need either 'import' or 'export' in the
 * file to be present for the feature to work
 * This way TS detects the file as module
 */
// export {}
